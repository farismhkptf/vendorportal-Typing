<!DOCTYPE html>

<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIP Medical Test Confirmed – First Edge Technologies</title>
    <style>
        :root {
            --color-black: #1d1d1f;
            --color-dark-gray: #3a3a3c;
            --color-medium-gray: #5e5e6a;
            --color-light-gray: #8e8e98;
            --color-lighter-gray: #a1a1a8;
            --color-background: #f5f5f7;
            --color-white: #ffffff;
            --color-border: #e8e8ed;
            --color-card-bg: #f8f8fc;
            --color-accent: #1d1d1f;
            --font-primary: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Arial, sans-serif;

```
        /* Avatar palette — lively, Apple-style */
        --avatar-amira-bg: #dbeafe;
        --avatar-amira-text: #1d4ed8;
        --avatar-shahul-bg: #dcfce7;
        --avatar-shahul-text: #15803d;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
        background-color: var(--color-background);
        font-family: var(--font-primary);
        line-height: 1.5;
        color: var(--color-black);
        -webkit-font-smoothing: antialiased;
        padding: 30px 20px;
    }

    .email {
        max-width: 720px;
        margin: 0 auto;
        background-color: var(--color-white);
        border-radius: 28px;
        box-shadow: 0 25px 50px -12px rgba(0,0,0,0.08);
        overflow: hidden;
    }

    .content {
        padding: 56px 48px;
    }

    /* ── HEADER ── */
    .header {
        display: flex;
        align-items: center;
        margin-bottom: 32px;
        padding-bottom: 16px;
        border-bottom: 1px solid var(--color-border);
    }

    .logo-placeholder {
        width: 36px;
        height: 36px;
        background-color: var(--color-accent);
        border-radius: 8px;
        margin-right: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--color-white);
        font-size: 17px;
        font-weight: 600;
        flex-shrink: 0;
    }

    .brand-wrapper {
        display: flex;
        flex-direction: column;
    }

    .brand {
        font-size: 17px;
        font-weight: 500;
        letter-spacing: -0.01em;
        color: var(--color-black);
        line-height: 1.3;
    }

    .tagline {
        font-size: 11px;
        font-weight: 400;
        color: var(--color-light-gray);
        letter-spacing: 0.02em;
    }

    /* ── HERO IDENTIFICATION ── */
    .company {
        font-size: 30px;
        font-weight: 500;
        letter-spacing: -0.015em;
        color: var(--color-black);
        margin-bottom: 6px;
        line-height: 1.2;
    }

    .service-type {
        font-size: 15px;
        font-weight: 400;
        color: var(--color-light-gray);
        margin-bottom: 28px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .vip-badge {
        display: inline-flex;
        align-items: center;
        background-color: var(--color-accent);
        color: var(--color-white);
        font-size: 10px;
        font-weight: 600;
        padding: 3px 9px;
        border-radius: 999px;
        text-transform: uppercase;
        letter-spacing: 0.06em;
    }

    /* ── APPLICANT ROW ── */
    .applicant-row {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-bottom: 28px;
        padding-bottom: 24px;
        border-bottom: 1px solid var(--color-border);
    }

    .avatar {
        width: 72px;
        height: 72px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 22px;
        font-weight: 600;
        letter-spacing: -0.02em;
        flex-shrink: 0;
        border: 2px solid var(--color-white);
        box-shadow: 0 4px 14px rgba(0,0,0,0.07);
    }

    .avatar-applicant {
        background-color: var(--avatar-amira-bg);
        color: var(--avatar-amira-text);
    }

    .avatar-guide {
        width: 52px;
        height: 52px;
        font-size: 17px;
        background-color: var(--avatar-shahul-bg);
        color: var(--avatar-shahul-text);
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .applicant-info { flex: 1; }

    .applicant-name {
        font-size: 26px;
        font-weight: 500;
        color: var(--color-black);
        margin-bottom: 3px;
        line-height: 1.2;
        letter-spacing: -0.01em;
    }

    .applicant-meta {
        font-size: 13px;
        color: var(--color-light-gray);
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .applicant-meta-dot {
        width: 3px;
        height: 3px;
        background: var(--color-lighter-gray);
        border-radius: 50%;
        display: inline-block;
    }

    .calendar-icons {
        display: flex;
        align-items: center;
        gap: 14px;
    }

    .calendar-icon {
        width: 26px;
        height: 26px;
        opacity: 0.65;
        transition: opacity 0.15s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
    }

    .calendar-icon:hover { opacity: 1; }
    .calendar-icon svg { width: 100%; height: 100%; }

    .appt-ref-value {
        font-size: 13px;
        font-weight: 500;
        color: var(--color-medium-gray);
        letter-spacing: 0.02em;
    }

    .map-link {
        display: inline-block;
        font-size: 13px;
        color: var(--color-black);
        text-decoration: none;
        border-bottom: 1px solid var(--color-border);
        padding-bottom: 2px;
        margin-top: 4px;
        transition: border-color 0.15s ease;
    }

    .map-link:hover { border-bottom-color: var(--color-black); }

    /* ── SECTION HEADING ── */
    .section-heading {
        font-size: 11px;
        font-weight: 600;
        color: var(--color-light-gray);
        text-transform: uppercase;
        letter-spacing: 0.07em;
        margin-top: 36px;
        margin-bottom: 14px;
    }

    /* ── APPOINTMENT DETAILS CARD ── */
    .guide-section {
        margin-bottom: 0;
        background: none;
        border: none;
        padding: 0;
    }

    .guide-row {
        display: flex;
        align-items: center;
        gap: 14px;
        margin-bottom: 10px;
    }

    .guide-info { flex: 1; }

    .guide-name {
        font-size: 18px;
        font-weight: 500;
        color: var(--color-black);
        margin-bottom: 1px;
    }

    .guide-role-inline {
        font-size: 13px;
        font-weight: 400;
        color: var(--color-light-gray);
        letter-spacing: 0.01em;
    }

    .guide-phone { font-size: 15px; }

    .guide-phone a {
        color: var(--color-medium-gray);
        text-decoration: none;
        border-bottom: 1px solid var(--color-border);
        transition: border-color 0.15s ease, color 0.15s ease;
    }

    .guide-phone a:hover {
        color: var(--color-black);
        border-bottom-color: var(--color-black);
    }

    .guide-description {
        font-size: 14px;
        color: var(--color-medium-gray);
        line-height: 1.6;
    }

    .bring-section { margin-bottom: 0; }

    .bring-card {
        background-color: var(--color-card-bg);
        border-radius: 20px;
        padding: 20px 22px;
        border: 1px solid var(--color-border);
    }

    .bring-item {
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 15px;
        font-weight: 500;
        color: var(--color-black);
    }

    .bring-check {
        width: 22px;
        height: 22px;
        background-color: var(--color-accent);
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: var(--color-white);
        font-size: 13px;
        font-weight: 600;
        flex-shrink: 0;
    }

    .bring-note {
        font-size: 13px;
        color: var(--color-light-gray);
        margin-left: 34px;
        margin-top: 6px;
    }

    /* ── AFTER APPOINTMENT ── */
    .after-section { margin-bottom: 0; }

    .after-text {
        font-size: 14px;
        color: var(--color-medium-gray);
        line-height: 1.6;
    }

    /* ── RESCHEDULE BOX ── */
    .reschedule-box {
        background-color: var(--color-card-bg);
        border-radius: 20px;
        padding: 18px 22px;
        margin: 36px 0 0;
        border: 1px solid var(--color-border);
    }

    .reschedule-text {
        font-size: 14px;
        color: var(--color-medium-gray);
        line-height: 1.6;
    }

    .reschedule-text strong {
        font-weight: 500;
        color: var(--color-black);
    }

    .reschedule-rm {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 6px;
        margin-top: 10px;
    }

    .reschedule-rm-name {
        font-size: 13px;
        font-weight: 500;
        color: var(--color-medium-gray);
    }

    .reschedule-rm-dot {
        width: 3px;
        height: 3px;
        background: var(--color-lighter-gray);
        border-radius: 50%;
        display: inline-block;
        flex-shrink: 0;
    }

    .reschedule-rm-link {
        font-size: 13px;
        color: var(--color-light-gray);
        text-decoration: none;
        border-bottom: 1px solid var(--color-border);
        transition: border-color 0.15s ease, color 0.15s ease;
    }

    .reschedule-rm-link:hover {
        color: var(--color-black);
        border-bottom-color: var(--color-black);
    }

    .reschedule-divider {
        height: 1px;
        background-color: var(--color-border);
        margin: 14px 0;
    }

    .reschedule-rm {
        display: flex;
        align-items: baseline;
        flex-wrap: wrap;
        gap: 6px 16px;
    }

    .reschedule-rm-name {
        font-size: 13px;
        font-weight: 500;
        color: var(--color-black);
    }

    .reschedule-rm-contacts {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
    }

    .reschedule-rm-link {
        font-size: 13px;
        color: var(--color-medium-gray);
        text-decoration: none;
        border-bottom: 1px solid var(--color-border);
        padding-bottom: 1px;
        transition: color 0.15s ease, border-color 0.15s ease;
    }

    .reschedule-rm-link:hover {
        color: var(--color-black);
        border-bottom-color: var(--color-black);
    }

    .reschedule-contacts {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-top: 14px;
        flex-wrap: wrap;
    }

    .reschedule-contact {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-size: 13px;
        font-weight: 500;
        color: var(--color-black);
        text-decoration: none;
        transition: opacity 0.15s ease;
    }

    .reschedule-contact:hover { opacity: 0.7; }

    .reschedule-icon {
        width: 26px;
        height: 26px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .reschedule-icon-call { background-color: #34c759; }
    .reschedule-icon-email { background-color: #007aff; }

    /* ── APPOINTMENT DETAILS CARD ── */
    .appt-card {
        background-color: var(--color-card-bg);
        border-radius: 20px;
        border: 1px solid var(--color-border);
        box-shadow: 0 2px 12px rgba(0,0,0,0.06);
        margin-bottom: 16px;
        overflow: hidden;
    }

    .appt-block {
        padding: 18px 22px;
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

    .appt-label {
        font-size: 11px;
        font-weight: 600;
        color: var(--color-light-gray);
        text-transform: uppercase;
        letter-spacing: 0.07em;
        margin-bottom: 2px;
    }

    .appt-datetime {
        font-size: 19px;
        font-weight: 600;
        color: var(--color-black);
        letter-spacing: -0.01em;
    }

    .appt-datetime-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        flex-wrap: wrap;
    }

    .appt-location-name {
        font-size: 15px;
        font-weight: 500;
        color: var(--color-black);
    }

    .appt-location-address {
        font-size: 13px;
        color: var(--color-medium-gray);
        line-height: 1.6;
    }

    .appt-divider {
        height: 1px;
        background-color: var(--color-border);
    }

    .appt-ref-row {
        padding: 14px 22px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        flex-wrap: wrap;
    }

    .appt-ref-value {
        font-size: 13px;
        font-weight: 500;
        color: var(--color-medium-gray);
        letter-spacing: 0.02em;
    }

    /* ── IMPORTANT NOTICE CARD ── */
    .notice-section { margin-bottom: 0; }

    .notice-card {
        background-color: var(--color-card-bg);
        border-radius: 20px;
        padding: 20px 22px;
        border: 1px solid var(--color-border);
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .notice-item {
        display: flex;
        align-items: flex-start;
        gap: 12px;
        font-size: 15px;
        font-weight: 500;
        color: var(--color-black);
        line-height: 1.5;
    }

    .notice-icon {
        width: 20px;
        height: 20px;
        flex-shrink: 0;
        margin-top: 1px;
        opacity: 0.5;
    }

    /* ── FOOTER ── */
    .footer {
        padding-top: 28px;
        border-top: 1px solid var(--color-border);
        text-align: center;
    }

    .footer-brand {
        font-size: 13px;
        font-weight: 500;
        color: var(--color-black);
        margin-bottom: 0;
        letter-spacing: -0.005em;
    }

    .footer-brand a {
        color: var(--color-black);
        text-decoration: none;
    }

    .footer-divider {
        width: 32px;
        height: 1px;
        background: var(--color-border);
        margin: 6px auto;
    }

    .footer-powered {
        font-size: 11px;
        color: var(--color-lighter-gray);
        margin-bottom: 4px;
    }

    .footer-powered a {
        color: var(--color-lighter-gray);
        text-decoration: none;
        border-bottom: 1px solid transparent;
        transition: border-color 0.15s ease, color 0.15s ease;
    }

    .footer-powered a:hover {
        color: var(--color-black);
        border-bottom-color: var(--color-border);
    }

    .footer-legal {
        font-size: 10px;
        color: var(--color-lighter-gray);
        letter-spacing: 0.01em;
    }

    /* ── MOBILE ── */
    @media only screen and (max-width: 600px) {
        .content { padding: 36px 24px; }
        .company { font-size: 26px; }
        .applicant-name { font-size: 22px; }
        .avatar { width: 60px; height: 60px; font-size: 19px; }
        .appt-datetime { font-size: 16px; }
        .appt-datetime-row { flex-direction: column; align-items: flex-start; }
        .guide-description { margin-top: 10px; }
    }

    /* ── DARK MODE ── */
    @media (prefers-color-scheme: dark) {
        body { background-color: #000; }

        .email {
            background-color: #1c1c1e;
            box-shadow: 0 25px 50px -12px rgba(255,255,255,0.04);
        }

        .header,
        .reference,
        .applicant-row,
        .footer { border-color: #2c2c2e; }

        .brand,
        .company,
        .applicant-name,
        .datetime,
        .location-name,
        .guide-name,
        .bring-item,
        .footer-brand { color: #f5f5f7; }

        .tagline,
        .service-type,
        .applicant-meta,
        .reference,
        .location-address,
        .guide-role,
        .guide-description,
        .bring-note,
        .after-text,
        .footer-powered,
        .footer-legal { color: #8e8e98; }

        .section-heading { color: #6e6e78; }

        .map-link,
        .guide-phone a,
        .footer-brand a,
        .footer-powered a { color: #f5f5f7; border-bottom-color: #3a3a3c; }

        .map-link:hover,
        .guide-phone a:hover,
        .footer-powered a:hover { border-bottom-color: #f5f5f7; }

        .reschedule-contact { color: #f5f5f7; }

        .bring-card,
        .notice-card,
        .appt-card,
        .reschedule-box {
            background-color: #2c2c2e;
            border-color: #3a3a3c;
        }

        .appt-divider { background-color: #3a3a3c; }
        .appt-datetime,
        .appt-location-name { color: #f5f5f7; }
        .appt-ref-value { color: #8e8e98; }

        .notice-item { color: #f5f5f7; }

        .notice-icon path,
        .notice-icon rect,
        .notice-icon circle { stroke: #f5f5f7; }

        .reschedule-text strong { color: #f5f5f7; }
        .reschedule-rm-name { color: #f5f5f7; }
        .reschedule-rm-link { color: #8e8e98; border-bottom-color: #3a3a3c; }

        .bring-check {
            background-color: #f5f5f7;
            color: #1c1c1e;
        }

        .footer-divider { background: #2c2c2e; }

        /* Avatar colors adjust for dark */
        .avatar-applicant {
            background-color: #1e3a8a;
            color: #93c5fd;
        }
        .avatar-guide {
            background-color: #14532d;
            color: #86efac;
        }
    }
</style>
```

</head>
<body>
<div class="email">
    <div class="content">

```
    <!-- HEADER -->
    <div class="header">
        <div class="logo-placeholder">K</div>
        <div class="brand-wrapper">
            <div class="brand">Keystone Business Solutions</div>
            <div class="tagline">Everything. In Order.</div>
        </div>
    </div>

    <!-- HERO IDENTIFICATION -->
    <div class="company">First Edge Technologies</div>
    <div class="service-type">
        Medical Test Appointment
    </div>

    <!-- APPLICANT ROW -->
    <div class="applicant-row">
        <div class="avatar avatar-applicant">AS</div>
        <div class="applicant-info">
            <div class="applicant-name">Amira Saleh</div>
            <div class="applicant-meta">
                Applicant
                <span class="applicant-meta-dot"></span>
                New Employment Visa
                <span class="applicant-meta-dot"></span>
                F26332
            </div>
        </div>
    </div>

    <!-- APPOINTMENT DETAILS CARD -->
    <div class="appt-card">

        <!-- Date & Time -->
        <div class="appt-block">
            <div class="appt-label">Date & Time</div>
            <div class="appt-datetime-row">
                <div class="appt-datetime">12 March 2026 · 11:00 AM</div>
                <div class="calendar-icons">
                    <a href="#" class="calendar-icon" title="Add to Google Calendar">
                        <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <rect x="3" y="4" width="18" height="17" rx="2" fill="white" stroke="#dadce0"/>
                            <rect x="3" y="4" width="18" height="5" rx="2" fill="#4285F4"/>
                            <rect x="3" y="7" width="18" height="2" fill="#4285F4"/>
                            <rect x="7" y="2" width="2" height="4" rx="1" fill="#4285F4"/>
                            <rect x="15" y="2" width="2" height="4" rx="1" fill="#4285F4"/>
                            <path d="M10 14.5L11.5 16L14.5 13" stroke="#34A853" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </a>
                    <a href="#" class="calendar-icon" title="Add to Apple Calendar">
                        <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <rect x="3" y="4" width="18" height="17" rx="2.5" fill="white" stroke="#d1d1d6" stroke-width="1"/>
                            <rect x="3" y="4" width="18" height="6" rx="2.5" fill="#ff3b30"/>
                            <rect x="3" y="8" width="18" height="2" fill="#ff3b30"/>
                            <rect x="7.5" y="2.5" width="1.5" height="3.5" rx="0.75" fill="#5e5e6a"/>
                            <rect x="15" y="2.5" width="1.5" height="3.5" rx="0.75" fill="#5e5e6a"/>
                            <text x="12" y="18" font-size="7" fill="#1d1d1f" text-anchor="middle" font-family="-apple-system,sans-serif" font-weight="600">12</text>
                        </svg>
                    </a>
                    <a href="#" class="calendar-icon" title="Add to Outlook Calendar">
                        <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <rect x="9" y="3" width="13" height="13" rx="1.5" fill="#0078D4"/>
                            <rect x="9" y="3" width="13" height="4" rx="1.5" fill="#005fa3"/>
                            <rect x="9" y="5" width="13" height="2" fill="#005fa3"/>
                            <rect x="12" y="2" width="1.5" height="3" rx="0.75" fill="#0078D4"/>
                            <rect x="18" y="2" width="1.5" height="3" rx="0.75" fill="#0078D4"/>
                            <rect x="2" y="8" width="11" height="13" rx="1.5" fill="#1d1d1f"/>
                            <text x="7.5" y="17" font-size="6" fill="white" text-anchor="middle" font-family="-apple-system,sans-serif" font-weight="600">OL</text>
                        </svg>
                    </a>
                </div>
            </div>
        </div>

        <div class="appt-divider"></div>

        <!-- Location -->
        <div class="appt-block">
            <div class="appt-label">Location</div>
            <div class="appt-location-name">Karama Medical Fitness Center</div>
            <div class="appt-location-address">Fajer Building, 25, 24 Street, Al Karama, Bur Dubai</div>
            <a href="#" class="map-link">View on Google Maps ↗</a>
        </div>

        <div class="appt-divider"></div>

        <!-- Medical Application -->
        <div class="appt-ref-row">
            <div class="appt-label">Medical Application</div>
            <div class="appt-ref-value">MED-2412-089456</div>
        </div>

    </div>

    <!-- YOUR ON-SITE GUIDE -->
    <div class="section-heading">Your On-Site Guide</div>
    <div class="guide-section">
        <div class="guide-row">
            <div class="avatar avatar-guide">SH</div>
            <div class="guide-info">
                <div class="guide-name">Shahul Hameed <span class="guide-role-inline">· On-Site Support</span></div>
                <div class="guide-phone">
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style="display:inline;vertical-align:middle;margin-right:4px;opacity:0.5"><path d="M6.62 10.79a15.05 15.05 0 006.59 6.59l2.2-2.2a1 1 0 011.01-.24c1.12.37 2.33.57 3.58.57a1 1 0 011 1V20a1 1 0 01-1 1C10.61 21 3 13.39 3 4a1 1 0 011-1h3.5a1 1 0 011 1c0 1.25.2 2.45.57 3.58a1 1 0 01-.25 1.01l-2.2 2.2z" fill="currentColor"/></svg>
                    <a href="tel:+971568115077">+971 56 811 5077</a>
                </div>
            </div>
        </div>
        <div class="guide-description">
            Shahul will meet your employee at the main entrance, bring all required documents, handle registration, and manage the queue. They simply need to be present.
        </div>
    </div>

    <!-- IMPORTANT NOTICE -->
    <div class="section-heading">Before You Go</div>
    <div class="notice-section">
        <div class="notice-card">
            <div class="notice-item">
                <svg class="notice-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="12" cy="12" r="9" stroke="#1d1d1f" stroke-width="1.5"/>
                    <path d="M12 7v5.5l3 2" stroke="#1d1d1f" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
                Arrive at least 10 minutes before your appointment time.
            </div>
            <div class="notice-item">
                <svg class="notice-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="4" y="3" width="12" height="16" rx="2" stroke="#1d1d1f" stroke-width="1.5"/>
                    <path d="M8 8h6M8 12h4" stroke="#1d1d1f" stroke-width="1.5" stroke-linecap="round"/>
                    <path d="M16 8h2a2 2 0 012 2v9a2 2 0 01-2 2H8a2 2 0 01-2-2v-1" stroke="#1d1d1f" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
                Bring your original passport. No copies or digital versions accepted.
            </div>
        </div>
    </div>

    <!-- AFTER THE APPOINTMENT -->
    <div class="section-heading">After the Appointment</div>
    <div class="after-section">
        <div class="after-text">Results are shared within 24 hours. We handle everything that follows — no action needed on your end.</div>
    </div>

    <!-- RESCHEDULE BOX -->
    <div class="reschedule-box">
        <div class="reschedule-text">
            <strong>Need to reschedule?</strong> Contact your Relationship Manager and we'll arrange a new slot at no cost.
        </div>
        <div class="reschedule-rm">
            <span class="reschedule-rm-name">Yasin Aboo</span>
            <span class="reschedule-rm-dot"></span>
            <a href="tel:+971566008833" class="reschedule-rm-link">
                <svg width="11" height="11" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style="display:inline;vertical-align:middle;margin-right:3px;opacity:0.45"><path d="M6.62 10.79a15.05 15.05 0 006.59 6.59l2.2-2.2a1 1 0 011.01-.24c1.12.37 2.33.57 3.58.57a1 1 0 011 1V20a1 1 0 01-1 1C10.61 21 3 13.39 3 4a1 1 0 011-1h3.5a1 1 0 011 1c0 1.25.2 2.45.57 3.58a1 1 0 01-.25 1.01l-2.2 2.2z" fill="currentColor"/></svg>+971 56 600 8833
            </a>
            <span class="reschedule-rm-dot"></span>
            <a href="/cdn-cgi/l/email-protection#2950485a404769595b464a46445948475007484c" class="reschedule-rm-link">
                <svg width="11" height="11" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style="display:inline;vertical-align:middle;margin-right:3px;opacity:0.45"><path d="M20 4H4a2 2 0 00-2 2v12a2 2 0 002 2h16a2 2 0 002-2V6a2 2 0 00-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z" fill="currentColor"/></svg><span class="__cf_email__" data-cfemail="e69f87958f88a696948985898b9687889fc88783">[email&#160;protected]</span>
            </a>
        </div>
    </div>

    <!-- FOOTER -->
    <div class="footer">
        <div class="footer-brand">
            <a href="https://www.procompany.ae">The P.R.O. Company™</a>
        </div>
        <div class="footer-divider"></div>
        <div class="footer-powered">
            Powered by <a href="https://www.keystonebusiness.ae">Keystone Business Solutions</a>
        </div>
        <div class="footer-legal">© 2026 The P.R.O. Company™. All rights reserved.</div>
    </div>

</div>
```

</div>
</body>
</html>