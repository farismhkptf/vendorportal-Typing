I have an existing app already built. I am creating a NEW app, but I want the NEW app to reuse the exact UI foundation from the existing app.

Your task:
1) ANALYZE my existing app’s UI layer and extract its complete design system into the new app.
2) Rebuild ONLY the UI shell + design system + routing skeleton. No business logic. No backend.

Hard requirements (must match the existing app exactly):
A) Login Page
- Recreate the login page exactly: same layout, spacing, background, typography, button styling, input styling, shadows, radius, and transitions.

B) Splash Screen
- Recreate the splash screen exactly: same background system, logo placement, loading behavior/animation (if any), typography and timing.

C) Global UI System (must be reusable)
- Create a centralized design system that matches the existing app exactly, including:
  - Font family + full typography scale (H1–H6, body, small, labels)
  - Color tokens (backgrounds, surfaces, text, borders, accents, states)
  - Spacing scale (e.g., 4/8/12/16/24/32… whatever the existing app uses)
  - Border radius scale
  - Shadow/elevation system
  - Motion rules (transition durations, easing, hover/press behavior)
  - Background wrapper (the same global background used across pages)

D) Reusable Components (extract into modules)
Create reusable components based on the existing app’s styling:
- <AppBackground /> (global background wrapper)
- <AuthLayout /> (layout wrapper for login/auth pages)
- <Button /> (primary/secondary/ghost variants if they exist)
- <Input /> (text/password, focus/invalid states)
- <Card /> or <Panel /> (if the existing app uses surfaces/cards)
- <Text /> or typography helpers (optional, only if it helps match exact text styles)

E) Routes / Pages (empty placeholders)
- Create all main routes as blank pages so I can build new content later.
- Each page should only have:
  - Correct global background + typography
  - A small page title
  - An empty container/card area (no real content)

F) Output requirements (IMPORTANT)
- Use a clean folder structure and keep the code scalable.
- Put design tokens in a single place (theme/tokens file).
- Use consistent naming so future pages automatically inherit the exact styling.
- The final result must look identical to my existing app’s login + splash, and all other pages must inherit the same look.

Do NOT:
- Do not change the visual style.
- Do not invent a new theme.
- Do not add features, data models, backend, or authentication logic beyond UI-only login screen.
- Do not add placeholder business text.

Deliverable:
A complete runnable app template that visually matches the existing app’s UI foundation, with login + splash fully implemented and all other routes as empty styled shells.