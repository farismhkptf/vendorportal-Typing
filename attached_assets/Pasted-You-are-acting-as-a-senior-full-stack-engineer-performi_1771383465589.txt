You are acting as a senior full-stack engineer performing a COMPLETE SYSTEM AUDIT of this application.

Your task is to carefully analyze the ENTIRE codebase — every file, page, component, route, and function.

I want you to check the following areas one by one:

1. CODE ERRORS
- Syntax errors
- Runtime risks
- Console warnings
- Incorrect imports
- Undefined variables
- Async issues
- API call problems
- State management mistakes
- Security risks (basic level)

2. STRUCTURE & CONNECTIONS
- Pages that exist but are not linked anywhere
- Buttons or actions that don’t navigate correctly
- Routes missing or misconfigured
- Components created but never used
- Dead code
- Duplicate logic
- Missing dependencies between modules
- Forms that don’t submit properly
- Broken data flow between frontend and backend

3. LOGIC & WORKFLOW VALIDATION
Check whether the app behaves correctly from a real user perspective:

- Login / authentication flow
- Role-based access (Admin, CRM, Vendor, etc.)
- Dashboard switching
- Work order lifecycle
- Status changes
- Notifications
- Queue logic
- Data saving and retrieval
- Edge cases (empty data, partial data, failures)

4. UI / UX FUNCTIONALITY
- Buttons that appear clickable but do nothing
- Loading states missing
- Error handling missing
- Inconsistent UI behavior
- Broken forms
- Incomplete user flows

5. DATABASE & BACKEND
- Schema mismatches
- Missing validations
- Incorrect queries
- Data not being persisted
- Relations not enforced
- Potential data corruption risks

6. PERFORMANCE & QUALITY
- Inefficient code
- Repeated API calls
- Unnecessary re-renders
- Large components that should be split
- Poor folder organization

OUTPUT FORMAT REQUIRED:

Give me a structured report with:

A. Critical Errors (must fix immediately)  
B. Functional Bugs  
C. Missing Connections / Broken Links  
D. Logic Problems  
E. Code Quality Improvements  
F. Suggested Enhancements  

For each issue provide:
- File name
- Problem
- Why it is a problem
- Exact fix recommendation

IMPORTANT:
Do NOT modify code yet.

First give me the FULL audit report.

After that I will ask you to implement fixes step by step.