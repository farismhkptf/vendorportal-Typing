Run a full-app diagnostics + improvement review (React/Tailwind website) — DISCUSS FIRST, DO NOT CHANGE ANY CODE YET.

HARD RULE
- Do NOT modify files.
- Do NOT “apply fixes.”
- This phase is analysis + proposal only.
- You may only make changes after I explicitly approve a specific plan.

GOAL
- Find anything broken, fragile, inconsistent, or hard to use.
- Even if it “works,” propose a simpler/better approach for UX + code.
- Optimize for ease, clarity, and clean structure.

SCOPE (DO ALL)
1) UI/UX Audit
- Navigation clarity (active states, mobile menu, spacing, CTA hierarchy)
- Typography + readability (line length, font sizes, contrast)
- Layout stability across breakpoints (no collapsed widths, no overflow)
- Forms (labels, validation, errors, loading states, success messages)
- Consistency (buttons, cards, headings, spacing scale)

2) Layout Diagnostics (VERY IMPORTANT)
- Identify any sections where text wraps character-by-character or containers collapse.
- In the Replit Preview iframe specifically, inspect computed widths and flex/grid rules.
- Pinpoint the exact root cause (which container/selector + why it collapses).
- Propose the fix pattern (min-w-0 / basis-0 / flex-1 / grid constraints), but do not apply it.

3) Accessibility (A11y)
- Semantic HTML (h1-h2 order, landmarks)
- Keyboard navigation + focus rings
- ARIA where needed
- Color contrast checks

4) Performance + Best Practices
- Image sizes + lazy loading
- Remove unused CSS/classes/components (recommendations only)
- Bundle/asset sanity checks

5) Code Quality + Simplification
- Identify duplicated UI patterns → propose reusable components (Button, Section, Card)
- Recommend better folder structure
- Flag hacks (global w-full bandaids) and propose correct replacements
- Identify tailwind/global CSS conflicts

DELIVERABLES (MUST RETURN)
A) A prioritized list:
   - P0 Critical (breaks layout/UX)
   - P1 Important (confusing/inconsistent)
   - P2 Nice-to-have (polish)
B) For EACH item include:
   - Location (file/component)
   - Root cause (what exactly is wrong)
   - Proposed fix (what to change)
   - Risk level (low/medium/high)
   - Estimated effort (S/M/L)
C) A “Simplify Plan” with 5–10 refactors to reduce complexity (proposal only)
D) A test checklist of what you would verify after fixes:
   - Desktop (wide), laptop, tablet, mobile
   - Chrome + Safari behavior (as much as possible)
   - Replit Preview iframe behavior

FORMAT
- Start with: “Top Findings (P0/P1/P2)”
- Then: “Root Causes”
- Then: “Proposed Fixes (Not Applied)”
- End with: “Approval Needed” — list the exact items you want me to approve.

REMINDER
No code changes until I approve.
