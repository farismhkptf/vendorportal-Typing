// The outer wrapper uses .login-fullscreen
// Layer order (back to front): gradient → photos → grain → overlay → content

<div className="login-fullscreen">
  {/* Animated gradient base (always shown) */}
  <div className="login-bg-gradient" aria-hidden="true" />

  {/* Optional: background photos that fade between slides */}
  {SLIDES.map((slide, i) => (
    <img
      key={i}
      src={slide.src}
      alt=""
      aria-hidden="true"
      className="login-bg-photo"
      style={{ opacity: i === currentSlideIndex ? 1 : 0, transition: 'opacity 0.8s ease-in-out' }}
    />
  ))}

  {/* Grain texture */}
  <div className="login-bg-grain" aria-hidden="true" />

  {/* Dark wash overlay so text is always readable */}
  <div className="login-bg-overlay" />

  {/* Content — z-10 to sit above all layers */}
  <div className="relative z-10 flex flex-col items-center justify-center min-h-screen px-4 py-8">
    <div className="w-full max-w-[400px]">

      {/* Glass card */}
      <div className="login-glass-card-v2 rounded-2xl overflow-hidden">
        <div className="px-8 pt-10 pb-8">

          {/* Logo */}
          <div className="flex flex-col items-center mb-8">
            <img src={yourLogo} alt="Logo" className="w-12 h-12 object-contain opacity-90 mb-5" />
            <h1 className="text-[22px] font-bold tracking-tight text-white mb-1.5">
              Your App Name
            </h1>
            <p className="text-sm text-white/60">Sign in to continue</p>
          </div>

          {/* Form */}
          <form onSubmit={handleLogin} autoComplete="on">
            <div className="space-y-4">
              {/* Username field */}
              <div className="space-y-1.5">
                <label className="text-xs font-medium text-white/70 uppercase tracking-wider">
                  Username
                </label>
                <input
                  type="text"
                  className="login-glass-input w-full rounded-md px-3 py-2"
                  placeholder="Enter your username"
                />
              </div>

              {/* Password field */}
              <div className="space-y-1.5">
                <label className="text-xs font-medium text-white/70 uppercase tracking-wider">
                  Password
                </label>
                <input
                  type="password"
                  className="login-glass-input w-full rounded-md px-3 py-2"
                  placeholder="Enter your password"
                />
              </div>
            </div>

            {/* Sign in button */}
            <div className="mt-6">
              <button type="submit" className="login-brand-button w-full py-2.5 rounded-md">
                Sign in
              </button>
            </div>

            {/* Footer note */}
            <p className="text-center text-[10px] text-white/40 mt-6">
              © 2026 Your Company — All Rights Reserved
            </p>
          </form>

        </div>
      </div>

    </div>
  </div>
</div>